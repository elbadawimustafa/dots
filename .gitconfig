[user]
	email = elbadawimustafa@gmail.com
	name = Mustafa
[push]
	default = current
[diff "gpg"]
	textconv = gpg --no-tty --decrypt
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
[credential "https://dev.azure.com"]
	useHttpPath = true

